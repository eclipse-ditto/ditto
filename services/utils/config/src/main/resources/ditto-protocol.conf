ditto.protocol {
  provider = "org.eclipse.ditto.services.utils.protocol.DittoProtocolAdapterProvider"
  blacklist = [
    "raw-request-uri",
    "cache-control",
    "connection",
    "timeout-access",
  ]
}
