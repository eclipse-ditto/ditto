mapper-limits {
  # maximum number of mappers defined in one source
  max-source-mappers = 2
  # maximum number of messages invoked by a mapper
  # defined in source
  max-mapped-inbound-messages = 3
  # maximum number of mappers defined in one source
  max-target-mappers = 4
  # maximum number of messages invoked by a mapper
  # defined in target
  max-mapped-outbound-messages = 5
}
