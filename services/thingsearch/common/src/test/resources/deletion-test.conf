deletion {
  enabled = false

  # delete "__deleted" Things older than:
  deletion-age = 2d
  # run each:
  run-interval = 23h
  # the first run should be at (UTC time):
  first-interval-hour = 20 # 21:00 UTC
}
