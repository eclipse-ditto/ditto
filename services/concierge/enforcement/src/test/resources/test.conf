ditto.concierge {
  caches {
    ask-timeout = 10s

    id {
      maximum-size = 80000
      expire-after-write = 15m
    }

    enforcer {
      maximum-size = 20000
      expire-after-write = 15m
    }
  }
}
