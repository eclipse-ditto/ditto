connection {
  encryption {
    enabled = true
    symmetrical-key = "dUHYs0YRphpLRnrge5W4ldskE7rI7ntU+x7csfyFWAM="
    json-pointers = ["/uri",
      "/credentials/key",
      "/sshTunnel/credentials/password",
      "/sshTunnel/credentials/privateKey",
      "/credentials/parameters/accessKey",
      "/credentials/parameters/secretKey",
      "/credentials/parameters/sharedKey",
      "/credentials/clientSecret"
    ]

    ## Client-certificate
    # "credentials.key"
    ## SSH tunneling
    # "sshTunnel.credentials.password"
    # "sshTunnel.credentials.privateKey"
    ## HMAC signing
    # "credentials.parameters.accessKey"
    # "credentials.parameters.secretKey"
    # "credentials.parameters.sharedKey"
    ## OAuth2 client credentials flow
    # "credentials.clientSecret"
  }
}
