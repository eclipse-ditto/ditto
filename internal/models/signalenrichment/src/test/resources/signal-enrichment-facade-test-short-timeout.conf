ditto {
  ask-with-retry {
    ask-timeout = 10ms
    retry-strategy = BACKOFF_DELAY
    retry-attempts = 3
    fixed-delay = 5ms
    backoff-delay {
      min = 10ms
      max = 100ms
      random-factor = 0.5
    }
  }
}

ask-with-retry-dispatcher {
  type = "Dispatcher"
  executor = "thread-pool-executor"
}