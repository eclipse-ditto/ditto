background-sync {
  enabled = false
  quiet-period = 1h
  idle-timeout = 2h
  keep {
    events = 3
  }
  throttle {
    throughput = 4
    period = 5h
  }
  min-backoff = 6h
  max-backoff = 7h
  max-restarts = 8
  recovery = 9h
  tolerance-window = 10h
  policy-ask-timeout = 11h
}